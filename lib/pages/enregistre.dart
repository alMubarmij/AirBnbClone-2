import 'package:flutter/material.dart';

class Enregistre extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Enregistre page'),
    );
  }
}
